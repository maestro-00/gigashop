services:
  catalogdb:
    image: postgres
  catalog.api:
    image: catalog.api
    build:
      context: .
      dockerfile: Services/Catalog/Catalog.API/Dockerfile
  basketdb:
    image: postgres 
    
volumes:
  postgres_catalog:
  postgres_basket:
